import 'dart:ui';

// Colors
const Color textColor = Color(0xFF000000); // Black
const Color bgColor = Color(0xFFFFFFFF); // White
const Color ringBGColor = Color(0xFF80C000); // Green
const Color ring1Color = Color(0xFF80C000); // Green
const Color ring2Color = Color(0xFF80C000); // Green
const Color ring3Color = Color(0xFF80C000); // Green
const Color secondaryBgColor = Color(0xFFFFFFFF); // White

// Sizes (if you need constant sizes)
const double smallPadding = 8.0;
const double mediumPadding = 16.0;
const double largePadding = 24.0;

// Animation durations
const Duration shortAnimationDuration = Duration(milliseconds: 200);
const Duration mediumAnimationDuration = Duration(milliseconds: 500);
const Duration longAnimationDuration = Duration(milliseconds: 1000);