class TranslationService {
  static Future<String> translateText(String text, String targetLang) async {
    // Implémentation temporaire - à remplacer par l'API réelle
    return "[Traduction] $text"; 
  }
}
